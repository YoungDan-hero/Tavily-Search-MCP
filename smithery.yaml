startCommand:
  type: stdio
  config:
    command: npx
    args:
      - "-y"
      - "github:YoungDan-hero/Tavily-Search-MCP"
  environmentVariables:
    - name: TAVILY_API_KEY
      description: Tavily Search API Key
      required: true