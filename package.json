{
  "name": "tavily-mcp-server",
  "version": "1.0.0",
  "author": "yangdan",
  "description": "Tavily Search MCP Server",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "tavily-mcp-server": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "esbuild index.js --bundle --platform=node --format=esm --minify --outfile=dist/index.js --banner:js=\"#!/usr/bin/env node\"",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@tavily/core": "^0.0.2",
    "@types/node": "^25.6.0",
    "esbuild": "0.28.0",
    "zod": "^3.23.0"
  }
}