{
  "name": "tavily-mcp-server",
  "version": "0.0.1",
  "author": "yangdan",
  "description": "Tavily Search MCP Server",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "tavily-mcp-server": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "ncc build index.js -o dist --minify --no-cache"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@tavily/core": "^0.0.2",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@vercel/ncc": "^0.38.4"
  }
}
